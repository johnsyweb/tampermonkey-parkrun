module.exports = {
  bundler: {
    // Opt-in list for scripts that should use the esbuild bundler path.
    scripts: ['p-index.user.js', 'p-index-progression.user.js'],

    // Template examples for future split scripts:
    // scripts: ['p-index.user.js', 'p-index-progression.user.js'],
  },
};
